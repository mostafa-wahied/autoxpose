port: 3000
serverIp: your-public-ip
lanIp: your-lan-ip

database:
  path: ./data/autoxpose.db

docker:
  socketPath: /var/run/docker.sock
  labelPrefix: autoxpose

dns:
  provider: netlify
  domain: example.com

proxy:
  provider: npm
  url: http://npm:81
